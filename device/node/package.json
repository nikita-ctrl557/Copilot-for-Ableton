{
  "name": "claude-copilot-node",
  "version": "0.1.0",
  "private": true,
  "description": "Node-for-Max agent for Claude Copilot. Runs inside the .amxd via node.script. API-key mode is dependency-free; subscription mode uses the Claude Agent SDK.",
  "type": "commonjs",
  "main": "main.js",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.168",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.4.3"
  }
}
